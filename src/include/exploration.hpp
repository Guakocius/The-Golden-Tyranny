#ifndef EXPLORATION_HPP

#include "game.hpp"

class Exploration {
    public:

    Exploration(Player player, std::map<std::string, )

    private:
    
    Player player;
    std::map<std::string,
};

#endif // !EXPLORATION_HPP